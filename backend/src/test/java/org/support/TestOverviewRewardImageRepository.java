package org.support;

import org.app.entity.*;
import org.springframework.data.jpa.repository.*;

public interface TestOverviewRewardImageRepository extends
        JpaRepository<OverviewRewardImage, Long> {

}
